"""
Should include auth helpers: either API keys or Bentley IMS validation.
- get_current_user() dependency fr protected  endpoints
- verify_jwt(token: str) to check JWTs from Bentley IMS
- API key validation for /mpc/optimize?
"""
