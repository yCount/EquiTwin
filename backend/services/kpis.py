"""
KPI (Key Performance indicator) computations for energy, comfort, and emissions.
- compute_kpis(range) → current power, daily kWh, CO₂ ppm, comfort score, baseline vs. actual savings
"""


def compute_kpis():
    # Stub — replace with real DB queries once training pipeline is wired in.
    return {
        "current_power_kw": None,
        "daily_kwh": None,
        "co2_ppm": None,
        "comfort_score": None,
        "savings_vs_baseline_pct": None,
    }
